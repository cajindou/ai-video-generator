# AI口播探店视频生成 - 改进方案

## 一、当前问题分析

### 1. 生成时间长
- **现状**：FFmpeg编码30秒视频需要约30秒
- **用户感知**：小程序端用户会等待很久
- **解决方案**：
  - 异步处理：生成任务放入队列
  - 进度显示：实时显示处理进度
  - 通知机制：生成完成后推送通知

### 2. 视频效果单一
- **现状**：只是图片轮播，没有字幕和音乐
- **用户感受**：视频单调，商业价值低
- **解决方案**：
  - 添加字幕：AI文案显示在视频底部
  - 背景音乐：可选的背景音乐
  - 转场效果：图片之间的过渡动画

### 3. 文案可能重复
- **现状**：AI生成的文案可能相似
- **解决方案**：
  - 提高温度参数(0.9)
  - 随机创意角度
  - 基于图片内容生成差异化文案

### 4. 视频无法预览
- **现状**：生成后没有播放界面
- **解决方案**：
  - 添加视频播放器页面
  - 支持在线预览
  - 支持下载分享

---

## 二、改进方案

### 方案A：基础版（免费）
- 图片轮播视频（无字幕）
- AI生成文案（显示在页面）
- 15秒视频
- 有水印

### 方案B：进阶版（VIP）
- 图片轮播 + 字幕
- AI配音（TTS）
- 背景音乐
- 30秒视频
- 无水印
- 高清下载

---

## 三、技术实现细节

### 3.1 视频生成流程

```typescript
// 1. 用户上传图片
POST /api/upload/image → 返回图片URL列表

// 2. 创建视频生成任务（异步）
POST /api/video/generate
{
  images: string[],      // 图片URL列表
  style: 'promotion' | 'store' | 'product',  // 风格选择
  duration: 15 | 30,     // 视频时长
  withSubtitle: boolean, // 是否添加字幕
  withMusic: boolean     // 是否添加音乐
}
→ 返回 taskId

// 3. 轮询查询进度
GET /api/video/task/:taskId
→ 返回 { status: 'processing', progress: 50, message: '正在生成视频...' }

// 4. 获取结果
GET /api/video/task/:taskId
→ 返回 { status: 'completed', videoUrl: '...', copywriting: '...' }
```

### 3.2 FFmpeg 命令改进

```bash
# 基础版：图片轮播（无字幕）
ffmpeg -loop 1 -t 10 -i image1.jpg \
       -loop 1 -t 10 -i image2.jpg \
       -loop 1 -t 10 -i image3.jpg \
       -filter_complex "[0:v]scale=1080:1920[v0];[1:v]scale=1080:1920[v1];[2:v]scale=1080:1920[v2];[v0][v1][v2]concat=n=3:v=1:a=0[v];anullsrc=r=44100:cl=stereo[a]" \
       -map "[v]" -map "[a]" output.mp4

# 进阶版：带字幕
ffmpeg -loop 1 -t 10 -i image1.jpg \
       -filter_complex "[0:v]scale=1080:1920,drawtext=text='字幕内容':fontsize=48:fontcolor=white:borderw=2:bordercolor=black:x=(w-text_w)/2:y=1700[v];anullsrc=r=44100:cl=stereo[a]" \
       -map "[v]" -map "[a]" output.mp4
```

### 3.3 前端交互流程

```
首页                    生成中                    结果页
┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│              │      │              │      │              │
│  上传图片    │      │  处理进度    │      │  视频预览    │
│  ┌────────┐  │      │  ████████░░  │      │  ┌────────┐  │
│  │ 图片1  │  │  →   │  80% 完成    │  →   │  │ 播放器 │  │
│  │ 图片2  │  │      │              │      │  └────────┘  │
│  │ 图片3  │  │      │  当前步骤：  │      │              │
│  └────────┘  │      │  生成字幕... │      │  [下载] [分享]│
│              │      │              │      │              │
│  [生成视频]  │      │              │      │  文案内容... │
└──────────────┘      └──────────────┘      └──────────────┘
```

---

## 四、预期效果

### 生成时间
| 视频时长 | 预计生成时间 |
|---------|------------|
| 15秒    | 约15秒     |
| 30秒    | 约30秒     |

### 视频质量
| 属性 | 规格 |
|-----|------|
| 分辨率 | 1080x1920 (竖屏) |
| 帧率 | 30fps |
| 编码 | H.264 |
| 音频 | AAC 44100Hz |

### 文案特点
- 每次生成不同（随机角度）
- 基于图片内容定制
- 符合口播节奏
- 有号召力

---

## 五、下一步行动

1. **立即修复**：FFmpeg参数错误（已修复）
2. **添加字幕**：drawtext滤镜实现
3. **优化文案**：提高AI创意性
4. **前端改进**：添加进度显示和视频播放器
5. **测试验证**：完整流程测试

