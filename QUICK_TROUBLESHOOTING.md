# 小程序扫码后无法使用 - 快速检查清单

## 问题现象
- ✅ 微信小程序审核通过
- ❌ 扫码后无法正常使用

## 快速检查清单

### 1. 检查后端服务是否运行

```bash
# 检查前端服务（端口 5000）
curl -I https://你的域名

# 检查后端服务（端口 3000，在生产环境中通常不会直接暴露）
# 如果使用 Coze 部署，后端服务会通过域名自动代理
```

**预期结果**：服务正常响应

---

### 2. 检查环境变量配置

检查 `.env.local` 文件：

```env
# 必须配置后端服务域名
COZE_PROJECT_DOMAIN_DEFAULT=https://你的域名
```

**如何获取域名**：
- **Coze 平台部署**：在 Coze 平台的部署页面查看
- **自己的服务器**：使用你自己的域名

---

### 3. 检查微信公众平台域名白名单配置

**配置路径**：
1. 登录 [微信公众平台](https://mp.weixin.qq.com/)
2. "开发" > "开发管理" > "开发设置" > "服务器域名"

**需要配置的域名**：
- `request` 白名单：`https://你的域名`
- `uploadFile` 白名单：`https://你的域名`
- `downloadFile` 白名单：`https://你的域名`（可选）

**注意事项**：
- ✅ 域名必须以 `https://` 开头
- ✅ 域名不能带端口号
- ❌ 不能使用 `localhost` 或 IP 地址

---

### 4. 检查微信开发者工具中的错误

**查看步骤**：
1. 打开微信开发者工具
2. 打开"调试器"
3. 查看 Console 和 Network 标签

**常见错误**：

| 错误信息 | 原因 | 解决方法 |
|---------|------|---------|
| `request:fail url not in domain list` | 域名未在白名单中 | 在微信公众平台配置域名白名单 |
| `request:fail -2 net::ERR_NAME_NOT_RESOLVED` | 域名无法解析 | 检查域名配置和 DNS 解析 |
| `request:fail -2 net::ERR_CONNECTION_REFUSED` | 服务无法连接 | 检查后端服务是否正常运行 |
| `request:fail -2 net::ERR_TIMED_OUT` | 请求超时 | 检查网络连接和服务响应时间 |

---

### 5. 检查是否重新构建了小程序

配置环境变量后，必须重新构建小程序：

```bash
# 重新构建微信小程序
pnpm build:weapp
```

**注意**：修改环境变量后，必须重新构建，否则配置不会生效！

---

### 6. 检查是否勾选了"不校验合法域名"

**开发环境**：
- 在微信开发者工具中
- 点击右上角"详情"
- 勾选"不校验合法域名"（仅用于开发调试）

**生产环境**：
- 必须配置域名白名单
- 不能勾选"不校验合法域名"

---

## 完整解决流程

### 第一步：获取后端服务域名

**Coze 平台部署**：
1. 登录 Coze 平台
2. 进入项目部署页面
3. 查看部署后的服务地址（域名）
4. 格式：`https://xxx-xxxx-xxx.coze.run`

**自己的服务器**：
1. 确保服务器可通过公网访问
2. 配置域名解析
3. 配置 SSL 证书
4. 格式：`https://api.yourdomain.com`

---

### 第二步：配置环境变量

编辑 `.env.local` 文件：

```env
# 后端服务域名（请替换为你的实际域名）
COZE_PROJECT_DOMAIN_DEFAULT=https://你的域名
```

---

### 第三步：重新构建小程序

```bash
pnpm build:weapp
```

---

### 第四步：在微信公众平台配置域名白名单

1. 登录 [微信公众平台](https://mp.weixin.qq.com/)
2. "开发" > "开发管理" > "开发设置" > "服务器域名"
3. 添加域名到白名单：
   - `request`：`https://你的域名`
   - `uploadFile`：`https://你的域名`
   - `downloadFile`：`https://你的域名`
4. 点击"保存"

---

### 第五步：测试验证

**使用微信开发者工具测试**：
1. 编译并预览小程序
2. 测试功能
3. 查看 Network 标签，确认请求正常

**使用真机扫码测试**：
1. 点击"预览"按钮
2. 扫描二维码
3. 测试功能
4. 确认功能正常

---

## 快速解决方案（最常见的情况）

如果你使用 Coze 平台部署，最快的解决方法是：

1. **获取 Coze 部署域名**
   - 在 Coze 平台的部署页面查看服务域名

2. **配置环境变量**
   ```env
   COZE_PROJECT_DOMAIN_DEFAULT=https://你的-Coze-域名
   ```

3. **重新构建**
   ```bash
   pnpm build:weapp
   ```

4. **配置域名白名单**
   - 在微信公众平台添加 Coze 域名到白名单

5. **测试验证**
   - 使用真机扫码测试

---

## 仍无法解决？

如果按照上述步骤仍然无法解决，请检查：

1. **域名是否正确**
   - 确认域名拼写正确
   - 确认域名可以正常访问（在浏览器中测试）

2. **SSL 证书是否有效**
   - 确认使用 HTTPS 协议
   - 确认 SSL 证书有效

3. **后端服务是否正常**
   - 确认后端服务正在运行
   - 确认 API 接口可以正常访问

4. **小程序代码是否正确**
   - 确认 `PROJECT_DOMAIN` 已正确配置
   - 确认重新构建了小程序

---

## 需要帮助？

如果问题仍未解决，请提供以下信息：

1. 后端服务域名是什么？
2. 微信公众平台配置的域名白名单是什么？
3. 微信开发者工具中显示的错误信息是什么？
4. Network 标签中显示的请求 URL 是什么？

---

## 相关文档

- [详细配置指南](./WECHAT_MINI_PROGRAM_DOMAIN_CONFIG.md)
- [微信小程序服务器域名配置文档](https://developers.weixin.qq.com/miniprogram/dev/framework/server-ability/network-request.html)
- [微信小程序网络请求文档](https://developers.weixin.qq.com/miniprogram/dev/api/network/request/wx.request.html)
